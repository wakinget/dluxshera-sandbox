# Core dependency: dLux from Dylan's fork
# Pinned to commit d0ab6df843503535c05225b15197233144be3cf1
# (last commit before ConvergingBeamOpticalSystem refactor)
dLux @ git+https://github.com/wakinget/dLux.git@d0ab6df843503535c05225b15197233144be3cf1

# dLux ecosystem
dLuxToliman==0.3.0

# JAX + numerical stack
jax==0.4.30
jaxlib==0.4.30
numpy>=1.24
scipy>=1.9

# Astronomy / units
astropy>=5.0

# Plotting / progress
matplotlib>=3.9
PyQt6
tqdm>=4.67
pandas>=1.5

# Inference / JAX helpers
numpyro==0.18.0
optax>=0.2.4
equinox>=0.11.10
chex>=0.1.88
jaxtyping>=0.2.36
# dLux at commit d0ab6df still imports zodiax.filter_vmap, which was removed in zodiax 0.5.
zodiax>=0.4.1,<0.5
interpax==0.3.12

# Notebook support (optional but nice for examples)
ipykernel>=6.31
jupyterlab>=4.4
ipywidgets
jupyterlab_widgets
